This is a sample web app written with ASP.NET 5 MVC to handle personal inventory and monthly expense lists. 
It covers basic principles of utilizing connections to a SQL Server and simple CRUD functions.
Utilizies Razor, Bootstrap, EntityFramework and the Package Manager Console in VS rather than .Net CLI in VSCode

As an example project, .env variables are not set up.
